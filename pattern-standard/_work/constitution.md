# Project Constitution

## Immutable Rules
- Do not add dependencies unless the spec explicitly allows it.
- Prefer small, reviewable changes per iteration.
- Keep `main` deployable; work happens on iteration branches.

## Documentation
- Update `_work/architecture.md` when architecture changes.
- Treat `_work/_NNNN-*/` folders as immutable history.
